<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:clipChildren="false"
    android:clipToPadding="false"
    android:paddingBottom="24dp"
    tools:background="@color/ghost_white">

    <androidx.constraintlayout.widget.Guideline
        android:id="@+id/start_guide_home"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        app:layout_constraintGuide_begin="16dp" />

    <androidx.constraintlayout.widget.Guideline
        android:id="@+id/end_guide_home"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        app:layout_constraintGuide_end="16dp" />

    <TextView
        android:id="@+id/hey_there_home"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:fontFamily="@font/open_sans_bold"
        android:text="Hey there!"
        android:textColor="@color/charcoal"
        android:textSize="26sp"
        app:layout_constraintEnd_toEndOf="@id/end_guide_home"
        app:layout_constraintStart_toStartOf="@id/start_guide_home"
        app:layout_constraintTop_toBottomOf="@id/drawer_home" />

    <TextView
        android:id="@+id/dont_forget_log_home"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:fontFamily="@font/open_sans_light"
        android:text="Don't forget to log your progress today."
        android:textColor="@color/lead"
        android:textSize="16sp"
        app:layout_constraintEnd_toEndOf="@id/end_guide_home"
        app:layout_constraintStart_toStartOf="@id/start_guide_home"
        app:layout_constraintTop_toBottomOf="@id/hey_there_home" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/calendar_recycler_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="16dp"
        android:clipChildren="false"
        android:clipToPadding="false"
        android:minHeight="88dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/dont_forget_log_home"
        tools:itemCount="7"
        tools:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
        tools:listitem="@layout/view_holder_date_home"
        tools:orientation="horizontal"
        tools:paddingStart="16dp"
        tools:paddingTop="8dp" />

    <androidx.appcompat.widget.AppCompatImageView
        android:id="@+id/drawer_home"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_marginTop="16dp"
        android:background="@drawable/bg_selectable_up_button"
        android:clickable="true"
        android:padding="16dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/ic_drawer2" />

</androidx.constraintlayout.widget.ConstraintLayout>